const LIFETIME_1M_CANDLES = 24 * 60 * 60; // 1 day, in seconds
const LIFETIME_5M_CANDLES = 30 * 24 * 60 * 60; // 1 month, in seconds

module.exports = {
  LIFETIME_1M_CANDLES,
  LIFETIME_5M_CANDLES,
};
